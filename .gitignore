# Python
__pycache__/
*.py[cod]
.pytest_cache/
.mypy_cache/
.ruff_cache/
.venv*/
venv/
env/

# Downloaded wheels / temp torch files
tmp_torch_dl/

# Build artifacts
build/
dist/
*.egg-info/

# IDE / OS
.DS_Store
Thumbs.db
.idea/
.vscode/

# Logs
*.log

# Local environment files
.env
.env.*

# Data — never committed (too large, local/VM only)
data/raw/
data/interim/
data/processed/frames/
data/processed/manifests/
data/processed/calibration/

# Large binary model files — use Git LFS or external storage (see README)
artifacts/checkpoints/
artifacts/exports/

# Failure frames — large image assets committed separately when ready
artifacts/failures/

# artifacts/metrics/ and artifacts/predictions/ ARE committed:
# they are small JSON/CSV files needed by collaborators for analysis (Phase 6)

# Android
android_app/.gradle/
android_app/build/
android_app/app/build/
