benchmark:
  target_device_fps: 15.0
  warmup_runs: 10
  timed_runs: 100
  batch_size: 1
  split: test

android:
  min_sdk: 26
  abi_filters:
    - arm64-v8a

reporting:
  levels:
    - frame
    - video
  threshold: 0.5
