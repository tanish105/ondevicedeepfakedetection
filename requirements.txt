# ── Environment ─────────────────────────────────────────────────────────────
# Active venv: .venv-py312 (Python 3.12)
# Install PyTorch separately — choose the right variant for your hardware:
#   CPU-only:   pip install torch torchvision --index-url https://download.pytorch.org/whl/cpu
#   CUDA 12.1:  pip install torch torchvision --index-url https://download.pytorch.org/whl/cu121
torch>=2.5.0
torchvision>=0.20.0

# ── Core data / ML ──────────────────────────────────────────────────────────
numpy>=1.26.0
pandas>=2.0.0
Pillow>=10.0.0
scikit-learn>=1.3.0
tqdm>=4.65.0

# ── Phase 2: preprocessing (frame extraction + face detection) ───────────────
opencv-python>=4.8.0
facenet-pytorch>=2.5.3

# ── Phase 4: export and quantization pipeline ────────────────────────────────
# PyTorch → ONNX → TensorFlow SavedModel → TFLite
onnx>=1.14.0
tensorflow>=2.16.0          # TFLite converter; Python 3.12 needs TF>=2.16
tf_keras                    # required by onnx2tf
onnx2tf>=1.20.0             # ONNX → TF SavedModel conversion
onnx-graphsurgeon>=0.3.0    # graph manipulation, required by onnx2tf
onnxruntime>=1.16.0         # ONNX parity verification
onnxoptimizer>=0.3.0        # graph optimisation used by onnx2tf
psutil>=5.9.0               # used by onnx2tf internals
sne4onnx>=1.0.0             # node extractor, required by onnx2tf
sng4onnx>=1.0.0             # node generator, required by onnx2tf
ai-edge-litert>=1.0.0       # TFLite runtime for local parity checks
